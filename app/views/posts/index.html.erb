<div class="container">
	<hr width = "100%">
	<div class="section">
	<div class="section">
	<div class="row">
		<div class="col s12 m12">
			<% @posts.each do |post| %>
				  <%= render partial: "post", object: post %>
			<% end %>
		</div>
	</div>
	<div class="center">
		<%= will_paginate @posts %>
	</div>
</div>

